package CinemaSystem.CinemaSystem.e2e;

/*
@SpringBootTest
@ExtendWith(SpringExtension.class)
public class MongoRepositoryTest {

    @Autowired
    private DocumentRepository documentRepository;

    @Autowired
    private DocumentFactory documentFactory;

    @Test
    void shouldGetDocumentFromMongo() {
        var document = documentFactory.create(new CreateDocumentCommand());
        documentRepository.put(document);

        var actualDocument = documentRepository.get(document.number());

        assertEquals(actualDocument.number(), document.number());
    }

}
*/